export const metadata = {
  title: 'YoDigitals',
  description: 'YoDigitals Store',
}

export default function RootLayout({ children }) {
 return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
